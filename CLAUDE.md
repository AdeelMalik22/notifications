# NotificationOS development context

## Current state

Phase 1 (foundation and operational readiness) plus the initial architecture
and tenancy slice are complete and pushed to `main`. The project is a Django + Django REST Framework modular monolith, not a
microservice system. The Django project package is named `notifications`.

The repository layout is deliberately flat:

```text
.
├── manage.py
├── notifications/       # Django project package
├── apps/                # Django domain apps
├── tests/
├── compose.yaml
└── plan.md
```

Do not add another wrapper directory around the project and do not create a
second nested `notifications/notifications/` package.

## Decisions already made

- Backend: Django 5.2 LTS, DRF, Celery, PostgreSQL, Redis, RabbitMQ.
- Architecture: one deployable codebase with separate web, migration, and
  worker processes; no microservices for the MVP.
- Async work: RabbitMQ is the Celery broker; Celery results are disabled.
  PostgreSQL will be the source of truth for notification and delivery state.
- Redis: cache, locks, and future rate-limit counters only; never the source of
  truth for delivery or idempotency state.
- Local email: Mailpit. Local SMS: deterministic fake adapter. The later real
  SMS provider is Twilio-compatible.
- Liveness (`/health/live`) checks only the web process. Readiness
  (`/health/ready`) checks PostgreSQL and Redis; it intentionally does not make
  API availability depend on RabbitMQ or workers.
- Project logs are JSON and request IDs are validated/generated by middleware.
  Do not log notification body content, recipient contacts, credentials, or
  provider secrets.
- Production settings fail closed for `DJANGO_SECRET_KEY` and
  `DJANGO_ALLOWED_HOSTS`.

## What has been implemented

1. Flat Django project scaffold, split settings, DRF defaults, OpenAPI schema
   and Swagger UI.
2. Health APIs, request-ID middleware, structured JSON logging, Celery smoke
   task, and fake SMS provider contract.
3. Docker Compose development stack:
   PostgreSQL, Redis, RabbitMQ, Mailpit, one-shot migrations, Gunicorn web, and
   Celery worker.
4. Developer workflow: `.env.example`, `Dockerfile`, `Makefile`, smoke script,
   README quickstart, and non-root container execution.
5. GitHub Actions CI for formatting, linting, type checks, 85% coverage,
   Django/production checks, migration drift, OpenAPI validation, real
   PostgreSQL/Redis/RabbitMQ checks, Celery task execution, Docker build, and
   Gunicorn liveness.
6. Architecture decision records and initial tenancy boundary: businesses,
   hashed API keys, scopes, expiry, revocation, tenant context, and scoped
   API-key management endpoints.
7. Initial notification catalogue and recipient boundary: categories, event
   types, channel templates and versions, recipients, preferences, tenant
   scoping, and mandatory-category validation.

The first CI run for this workflow passed:
<https://github.com/AdeelMalik22/notifications/actions/runs/29990927486>.

## Important commands

```bash
make install        # install exactly from uv.lock
make check          # local formatting/lint/type/test/schema/Compose gates
make ci             # stricter host-side gate, including coverage and deploy checks
make up             # build and start the local stack
make smoke          # probe endpoints, infrastructure, and the Celery worker
make migrate        # run the one-shot migration container
make down           # stop stack while preserving developer volumes
```

For a clean local start:

```bash
cp .env.example .env
make up
make smoke
```

## Git workflow

- Make focused, reviewable commits. Do not manufacture empty or artificial
  commits just to increase contribution counts.
- Push each completed, validated slice to `origin/main`.
- Never use `git add .` in this repository without first reviewing untracked
  files. The source PRD is intentionally untracked:
  `NotificationOS_PRD_v2.md`.
- Do not commit `.env`, `.venv`, IDE settings, local databases, generated
  coverage, or real credentials.
- Existing Phase 1 commits are:

  - `d9b764f` scaffold Django project foundation
  - `f408a8e` health and readiness endpoints
  - `ff9d280` structured request logging
  - `452a7cf` fake SMS provider adapter
  - `7af7c25` local development infrastructure
  - `6021dd4` Phase 1 CI quality gates

## Implementation conventions

- Keep business writes in explicit service functions; serializers validate
  transport data and selectors own reads.
- Avoid business-critical Django signals.
- Every tenant-owned table must have a non-null `business_id`; derive tenant
  identity from authentication, never from a request body.
- Use JSON-only Celery payloads with opaque row IDs and trace metadata. Never
  put rendered content, contacts, credentials, or other PII in broker messages.
- New API endpoints go under `/api/v1/`; retain OpenAPI coverage and tests.
- Add migrations with the model change and always run
  `python manage.py makemigrations --check --dry-run` before committing.
- Unit tests use in-memory SQLite and locmem cache. Any behavior relying on
  PostgreSQL, Redis, RabbitMQ, or a worker needs a real integration test.

## Remaining work

The detailed roadmap is in `plan.md`. Build the next slices in this order,
with migrations, tests, documentation, focused commits, and pushes for each.

1. **Complete architecture decisions and tenancy**
   - Add API-key rotation and audit events for sensitive key operations.
2. **Complete notification catalogue and recipients**
   - Add template publishing/preview, safe restricted rendering, and full
     update/delete lifecycle actions.
3. **Trigger and durable delivery pipeline**
   - Implement `POST /api/v1/notifications` with required idempotency keys.
   - Store notification, delivery snapshots, and outbox rows in one database
     transaction.
   - Add an outbox relay and idempotent Celery delivery tasks.
4. **Real providers and delivery history**
   - Add SMTP email and Twilio-compatible SMS adapters.
   - Encrypt provider credentials; record delivery attempts and fixed retries
     (initial send plus two retries, 60 seconds apart).
5. **Safety and operations**
   - Add tenant admission limits, recipient rate limits, queue fairness, audit
     records, retention/deletion/anonymization jobs, metrics, tracing context,
     backups, and recovery verification.
6. **API and acceptance testing**
   - Complete `/api/v1/` CRUD resources and internal Django Admin support.
   - Add integration/contract/e2e tests for tenant isolation, idempotency,
     retries, preference handling, provider failures, and PII-safe logs.

Before changing scope, compare the request against `plan.md` and
`NotificationOS_PRD_v2.md`; document any material product decision in an ADR.
